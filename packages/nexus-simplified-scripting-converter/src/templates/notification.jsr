{
  let title = {{:~json(heading)}}
  if(args){
    title = do_replace(title, args)
  }
  let text = {{:~json(text)}}
  if(args){
    text = do_replace(text, args)
  }
  nexusclient.ui().notifications().notify(title, text);
}
