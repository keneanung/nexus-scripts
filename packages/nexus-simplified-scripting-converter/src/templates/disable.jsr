{
  const reflex = nexusclient.reflexes().find_by_name({{:~json(type)}}, {{:~json(name)}}, true, false, current_package);
  if(!reflex){
    nexusclient.display_notice('Disable reflex: unable to find ' + {{:~json(type)}} + ' ' + {{:~json(name)}} + ' in ' + (current_package?current_package:'global reflexes'), 'red');
  }else{
    nexusclient.reflexes().disable_reflex(reflex);
  }
}
